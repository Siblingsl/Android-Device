{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "device-window-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default",
    "updater:default"
  ]
}
