# Dashboard 网格对齐实施计划

## 阶段 1：锁定布局契约

- 在现有布局测试中增加 Dashboard 网格间距和分栏计算断言。
- 先运行该测试，确认当前 `1.35fr / 0.65fr` 规则按预期失败。

## 阶段 2：最小前端修复

- 只修改 `src/styles/global.css` 的 Dashboard 网格规则。
- 通过共用间距和精确的第一列宽度计算，让两行网格共享分栏线。
- 保留既有媒体查询和其余页面样式。

## 阶段 3：验证

- 运行布局测试、TypeScript 检查、全量 Vitest。
- 运行 `src-tauri` 与 `qemu-center` 的 Rust 测试，确认前端改动未引入跨层回归。
- 视觉效果仍标记为待人工确认。
