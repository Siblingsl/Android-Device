# 容器与节点合并页 P7-1 真机人工走查清单

> 这是一份人工验收记录，不是自动化测试。默认状态全部为“未执行”；只有在真实 Tauri 应用中完成操作并保存证据后，才能改为“通过”。

## 记录信息

| 项目 | 记录 |
|---|---|
| 应用版本 | 未执行 |
| Windows 版本/build | 未执行 |
| CPU/内存 | 未执行 |
| 日期与操作者 | 未执行 |
| Docker Desktop 状态 | 未执行 |
| QEMU node1 状态 | 未执行 |
| 截图/日志目录 | 未执行 |

状态值：`未执行` / `通过` / `不通过` / `不适用`。

## C. 页面骨架与导航

| 编号 | 操作 | 期望结果 | 状态 | 实际结果/证据 |
|---|---|---|---|---|
| C-01 | 启动真实 Tauri 应用并打开 Dashboard | 应用窗口正常出现，没有浏览器预览提示 | 未执行 | |
| C-02 | 查看侧边栏 | 只出现一个“容器与节点”入口，不再出现 Docker/QEMU 两个并列入口 | 未执行 | |
| C-03 | 点击“容器与节点” | 地址为 `/containers`，页面标题和副标题只出现一份 | 未执行 | |
| C-04 | 直接打开旧 `/docker` 地址 | 自动进入 `/containers?track=docker` 或等价合并页 Docker 状态 | 未执行 | |
| C-05 | 直接打开旧 `/qemu` 地址 | 自动进入 `/containers?track=qemu` 或等价合并页 QEMU 状态 | 未执行 | |
| C-06 | 检查轨道切换控件 | 控件具有 tablist/tab/tabpanel 语义，当前 tab 有选中状态 | 未执行 | |
| C-07 | 点击“本机 Docker” tab | URL 保留 `/containers` 并带 `track=docker`，Docker 面板可见 | 未执行 | |
| C-08 | 点击“QEMU 节点” tab | URL 带 `track=qemu`，QEMU 面板可见，实验性提示仍存在 | 未执行 | |
| C-09 | 切换轨道后刷新应用 | 刷新后仍回到 URL 指定的轨道，不能丢失选择 | 未执行 | |
| C-10 | 只设置记忆偏好后打开 `/containers` | 没有 URL 轨道时使用记忆偏好；首次无偏好时回到 Docker | 未执行 | |
| C-11 | 打开 `?track=qemu&view=compare` | 直接进入只读对比视图，轨道和视图状态都保留 | 未执行 | |
| C-12 | 从设备中心、设置或其他页面返回 | 返回路径进入合并页正确轨道，不出现旧页面头 | 未执行 | |

## D. 两条轨道功能面

### D1. Docker 轨道

| 编号 | 操作 | 期望结果 | 状态 | 实际结果/证据 |
|---|---|---|---|---|
| D-01 | 在 Docker tab 观察环境状态 | Docker 引擎、WSL/binder 状态来自真实检查；不可用时说明原因 | 未执行 | |
| D-02 | 打开创建实例表单 | 表单、默认值和验证与原 Docker 页面一致 | 未执行 | |
| D-03 | 创建无 GApps 基础实例 | 显示阶段进度，成功后设备/容器出现在 Docker 面板 | 未执行 | |
| D-04 | 创建过程中切到 QEMU 再切回 | Docker 创建任务不丢失，进度和按钮状态连续 | 未执行 | |
| D-05 | 在创建过程中取消或触发失败路径 | 显示真实失败原因，已创建的临时资源按原逻辑处理 | 未执行 | |
| D-06 | 使用名称或 ADB 端口冲突值 | 页面指出冲突并阻止提交，不静默覆盖已有实例 | 未执行 | |
| D-07 | 使用搜索、筛选和实例检查/日志 | 筛选不改变真实数据，日志入口打开对应实例信息 | 未执行 | |
| D-08 | 从 Docker 面板进入设备详情 | 设备详情 serial、来源、启动/停止和控制动作对应 Docker 实例 | 未执行 | |
| D-09 | 查看 Docker 来源徽标/数量 | 数量和状态取自 Docker 轨道真实读取，不显示伪造的 0 | 未执行 | |
| D-10 | 切换自定义 binder 或恢复默认 | 页面明确提示权限、重启 Docker Desktop 或风险，不隐藏系统操作成本 | 未执行 | |
| D-11 | 打开 Volumes 入口并返回 | 数据卷跳转正常；删除容器不会被误解为删除数据卷 | 未执行 | |
| D-12 | 制造 Docker 不可用状态后返回页面 | 操作按钮禁用或给出可执行修复提示，页面不假装可用 | 未执行 | |

### D2. QEMU/WHPX 轨道

| 编号 | 操作 | 期望结果 | 状态 | 实际结果/证据 |
|---|---|---|---|---|
| D-13 | 在 QEMU tab 运行环境体检 | 八项体检显示 ok/fail/unknown，实验性和缺失 CLI 提示清晰 | 未执行 | |
| D-14 | 查看 QEMU 状态目录提示 | 状态目录、便携 QEMU 和日志位置可理解，不指向错误路径 | 未执行 | |
| D-15 | node1 运行时切到 Docker 再切回 | QEMU 节点状态和实例列表没有被 Docker 面板覆盖 | 未执行 | |
| D-16 | 启动/停止 node1 | 启动和停止按钮状态正确；停止使用优雅流程，不要求硬杀 | 未执行 | |
| D-17 | 打开节点验收 | verify 七项区分 PASS、FAIL、UNTESTED；UNTESTED 不显示为失败 | 未执行 | |
| D-18 | 创建 QEMU 节点或打开已有节点 | 节点配置、端口块、镜像和 auto-setup 状态可见且不混入 Docker 表单 | 未执行 | |
| D-19 | 等待 guest SSH | 显示等待进度、可取消/可恢复；切页后任务不丢失 | 未执行 | |
| D-20 | 创建无 GApps QEMU 实例 | 实例出现在当前节点，serial 和端口可复制，ADB 状态最终正确 | 未执行 | |
| D-21 | 使用 GApps、Magisk/Zygisk 等预装 | 表单依赖关系和版本限制正确；失败时显示真实阶段和原因 | 未执行 | |
| D-22 | 执行一次预装升级 | 升级期间按钮锁定、进度连续，数据保留结果可观察 | 未执行 | |
| D-23 | 执行恢复升级前 | 恢复结果、旧实例/数据卷和 ADB 状态清晰，不误报成功 | 未执行 | |
| D-24 | 查看命令日志、`qemu.log` 和 `console.log` 入口 | 三类日志来源可区分，卡死排障提示指向正确文件 | 未执行 | |
| D-25 | 在 QEMU 长任务运行时离开并返回 | 常驻任务条、进行中状态和返回后的面板状态一致 | 未执行 | |

### D3. 对比视图

| 编号 | 操作 | 期望结果 | 状态 | 实际结果/证据 |
|---|---|---|---|---|
| D-26 | 从 Docker 或 QEMU 面板进入对比 | URL 带当前 `track` 和 `view=compare`，页面明确只读 | 未执行 | |
| D-27 | 检查实例数、运行数和健康度 | 两条轨道各自显示来源和口径；QEMU 说明当前所选节点范围 | 未执行 | |
| D-28 | 检查 CPU/内存配额、磁盘、启动耗时 | 有数据则显示真实值；无数据则显示“不可用/无数据源”，不显示 0 | 未执行 | |
| D-29 | 尝试在对比视图中寻找写操作 | 不出现创建、删除、启动、停止等写操作；刷新回到轨道面板处理 | 未执行 | |

## E. 无障碍与视觉

| 编号 | 操作 | 期望结果 | 状态 | 实际结果/证据 |
|---|---|---|---|---|
| E-01 | 只用键盘从页面入口开始导航 | 主要操作都能到达，Tab 顺序符合视觉顺序 | 未执行 | |
| E-02 | 在 tablist 上使用左右方向键 | 轨道切换正确，焦点停留在新 tab | 未执行 | |
| E-03 | 观察当前 tab 和 tabpanel | 选中态、焦点态、面板关联清楚，不只依赖颜色 | 未执行 | |
| E-04 | 查看所有禁用按钮 | 禁用原因可理解，不出现无理由灰色按钮 | 未执行 | |
| E-05 | 触发 Docker 和 QEMU 错误状态 | 错误文字完整、可读，操作入口不被错误遮挡 | 未执行 | |
| E-06 | 观察空数据和加载状态 | skeleton、空态和重新检查入口稳定，不发生布局跳动 | 未执行 | |
| E-07 | 观察来源徽标和状态徽标 | 16px 图标可辨识，图标旁有文字或辅助语义 | 未执行 | |
| E-08 | 缩窄窗口到最小宽度附近 | 页头、tab、表单和表格不重叠，水平滚动有合理边界 | 未执行 | |
| E-09 | 切换中文与英文 | 文案不截断、不溢出，状态和按钮语义仍完整 | 未执行 | |
| E-10 | 检查深色/浅色主题 | 文字、边框、禁用态和警告态具有足够对比度 | 未执行 | |
| E-11 | 检查页面头部和卡片层级 | 合并页只有一个清晰页头，轨道内部标题不造成重复层级 | 未执行 | |
| E-12 | 从错误状态恢复 | 重新检查、返回轨道和重新进入页面后焦点与状态合理 | 未执行 | |

## 结论

| 项目 | 结果 |
|---|---|
| C 页面骨架与导航 | 未执行 |
| D 两条轨道功能面 | 未执行 |
| E 无障碍与视觉 | 未执行 |
| 阻塞问题 | 未执行 |
| 走查结论 | 未执行 |

只有 C / D / E 都完成且阻塞问题已经修复或登记后，才可在发布文档中引用本次走查结果。
